[build]
  base    = "web"
  command = "jekyll build"
  publish = "_site"

[build.environment]
  JEKYLL_ENV = "production"

# Override baseurl for Netlify (served from root, not /football-analytics)
[[redirects]]
  from = "/*"
  to   = "/:splat"
  status = 200
